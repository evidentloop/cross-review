fixture_id: "012-hermes-tui-model-picker"
source: assistant_local_review
reviewer_model: "claude-opus-4.6"
reviewed_at: "2026-05-16T18:00:00+08:00"
commit: "ddd28329"
repo: "hermes-agent"

context_items:
  - type: diff
    path_or_desc: "prompt-lab/cases/012-hermes-tui-model-picker/diff.patch"
    required: true
    covered_by_pack: null

findings: []
# Clean/near-clean case. The regression fix removes the buggy code path that
# overwrote curated model lists with live /models catalog data (which pulled
# in non-agentic models like image generators, embeddings, TTS). The fix simply
# removes the overwriting import and usage, letting list_authenticated_providers
# serve as the single truth source. Two well-targeted regression tests pin the
# behavior. The diff is a pure correction with no new logic to get wrong.
